components:
  schemas:
    timeBasis:
      $ref: ../../../enum/TimeUnit.yaml#/components/schemas/TimeUnit
      description: >-
        Die Zeit(dauer) auf die sich der Preis bezieht. Z.B. ein Jahr für einen
        Leistungspreis der in €/kW/Jahr

        ausgegeben wird.
