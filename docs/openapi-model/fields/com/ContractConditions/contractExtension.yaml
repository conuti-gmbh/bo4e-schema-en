components:
  schemas:
    contractExtension:
      $ref: ../../../com/TimePeriod.yaml#/components/schemas/TimePeriod
      description: >-
        Falls der Vertrag nicht gekündigt wird, verlängert er sich automatisch
        um die hier angegebene Zeit. Details

        Zeitraum
