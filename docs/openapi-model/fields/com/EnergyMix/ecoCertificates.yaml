components:
  schemas:
    ecoCertificates:
      type: array
      items:
        $ref: ../../../enum/EcoCertificate.yaml#/components/schemas/EcoCertificate
      description: Zertifikate für den Energiemix
      nullable: true
