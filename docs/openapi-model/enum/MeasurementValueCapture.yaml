components:
  schemas:
    MeasurementValueCapture:
      title: MeasurementValueCapture
      type: string
      enum:
        - REMOTE_READABLE
        - MANUALLY_READ
      description: Die Messwerterfassung des Zählers
